on: push
jobs:
  build-node:
    runs-on: ubuntu-latest
    container: node:16
    steps:
      run: node --version
      run: pnpm --version
      uses: actions/checkout@v3
      run: pnpm install
      run: node app.js
